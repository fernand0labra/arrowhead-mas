REQUEST
    <static> Communication Protocol :: HTTP
    Encoding Format :: XML
    
RESPONSE
    <static> Communication Protocol :: HTTP
    Encoding Format :: XML
    Semantics
        <supposed> Standard :: SenML
        Notation
            v : temperature
            u : unit
            t : time