REQUEST
    <static> Communication Protocol :: HTTP
    Encoding Format :: JSON
    
RESPONSE
    <static> Communication Protocol :: HTTP
    Encoding Format :: JSON
    Semantics
        <supposed> Standard :: No standard
        Notation
            temperature
            unit
            time